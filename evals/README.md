# Agent Evaluation

The canonical Agent Evaluation documentation has moved to:

[`docs/agent-evaluation.md`](../docs/agent-evaluation.md)

This file is intentionally kept as a short entry point for users navigating from the `evals/` directory. Keeping a single canonical guide avoids duplicated evaluation documentation drifting out of sync.

## Current scope

The project currently includes real-agent evaluation coverage for:

- Tool selection accuracy
- Outcome / task success

The broader evaluation roadmap, architecture, planned metrics, dataset design, and regression strategy are documented in the canonical guide.